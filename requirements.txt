# 본편 파이프라인 (code/p*.py, reproduce/)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14.0          # parquet 입출력
# 신경망 예측기 (p2의 MLP/LSTM — 없으면 해당 모델만 건너뜀)
torch>=2.0
# 데이터 빌드 전용 (FnGuide 원시 파일 보유 시에만 필요)
python-calamine>=0.2   # PBR.xlsx 고속 파싱 (openpyxl 대비 ~15배)
openpyxl>=3.1
# 노트북·문서 빌드
jupyter
nbformat
markdown
